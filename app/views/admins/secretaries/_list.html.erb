<div class="container-fluid">
  <br>
  <table class="table table-bordered table-hover table-striped table-sm">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Status</th>
      <th scope="col">Options</th>
    </tr>
  </thead>
  <tbody>
    <% secretaries.each do |secretary| %>
      <tr>
        <td><%= link_to secretary.full_name, admins_secretary_path(secretary) %></td>
        <td><%= secretary.status %></td>
        <td>
            <div class="dropdown">
                <button type="button" class="btn btn-dark btn-sm dropdown-toggle" data-toggle="dropdown">Actions</button>
                <div class="dropdown-menu">
                    <%= link_to "View", secretary, :class => "dropdown-item" %>
                    <%= link_to "Edit", edit_admins_secretary_path(secretary), :class => "dropdown-item" %>
                    <%= link_to "Delete", secretary, method: :delete, data: { confirm: "¿Deseas eliminar al administrador?" }, :class => "dropdown-item" %>
                </div>
            </div>
        </td>
      </tr>    
    <% end %>
  </tbody>
  </table>
</div>