<div class="dropdown-divider"></div>

<div class="container-fluid">
  <%= link_to 'New Teacher', new_teacher_path, {:class => "btn btn-dark"} %>
  <br>
  <table class="table table-bordered table-hover table-striped table-sm">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Email</th>
      <th scope="col">Status</th>
      <th scope="col">Options</th>
    </tr>
  </thead>
  <tbody>
    <% teachers.each do |teacher| %>
      <tr>
        <th scope="row"><%= teacher.full_name %></th>
        <td><%= teacher.email %></td>
        <td><%= teacher.status %></td>
	<td>
            <div class="dropdown">
                <button type="button" class="btn btn-dark btn-sm dropdown-toggle" data-toggle="dropdown">Acciones</button>
                <div class="dropdown-menu">
                    <%= link_to "Mostrar", teacher, :class => "dropdown-item" %>
                    <%= link_to "Editar", edit_teacher_path(teacher), :class => "dropdown-item" %>
                    <%= link_to "Eliminar", teacher, method: :delete, data: { confirm: "¿Deseas eliminar al profesor?" }, :class => "dropdown-item" %>
                </div>
            </div>
        </td>
      </tr>    
    <% end %>
  </tbody>
</table>
